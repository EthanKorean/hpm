<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapper" >
	
	<select id="authUser" resultType="AuthSuccessDomain">
		SELECT 	_id   userId
				,name userName
		FROM USER WHERE EMAIL='sm000@ocube.co.kr'
	</select>


</mapper>